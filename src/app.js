(function() {
    'use strict';

    // creating the app injecting the external modules required by the app
    angular.module('myApp', ['ui.router', 'ngMaterial', 'ngMessages', 'partials', 'ngStorage']);

})();